# 指示

## 概要

PDF形式の書籍を読みながらチャットで質問できるPC用のデスクトップアプリケーションを作りたい。

## 求めるユーザー体験

アプリの左半分でPDFを読む。  
気になるところをマウスで選択した状態で右半分のチャットUIで質問すると、選択した状態のテキストだけをコンテキストに含めて解凍してくれる。

目指すのは[こんな感じ](https://x.com/gorilla0513/status/2086303282851725505/photo/1)

## アーキテクチャについて

* Tauri (https://v2.tauri.app/ja/) を使ったデスクトップアプリケーションとする。
* バックエンドではACP (Agent Client Protocol) を通じてOpenCodeとやりとりする

## 技術要件

* フロントエンドはNext.js v16.3 (https://nextjs.org/)、バックエンドはTauriの制約上Rust
* 画面の左半分ではEmbedPDF (https://www.embedpdf.com/docs)によるPDF Viewerを起動
* 画面の右半分ではassistant-ui (https://www.assistant-ui.com/) を使ったチャットUIを起動
* フロントエンドとバックエンドのやりとりはTauriの標準に従う

## ほしい機能

* PDFのファイル選択 ... Tauriの機能を利用して、読むPDFを選択する
* PDF Viewer ... 画面の左側でPDFが読める
* テキスト選択 ... 非常に重要な機能。マウスで選択したテキストだけを、後述のチャットのコンテキストに含める必要がある。
* チャットUI ... 画面の右半分で使う。ACPを通じてOpenCodeとやりとりする。OpenCodeのモデル選択ができると嬉しい。

## 作業の進め方

* 並列して進められるタスクについては、herdrでworktreeを切って同時に作業してよい。
* worktree内ではCursor CLI (`agent`コマンド) を起動して作業すること。
* `develop` ブランチに対してはレビュー（敵対的検証）が通ればローカルでガンガンマージして良い。
* 適切な粒度でcommitをまとめること。
* 不明点があれば解消するまでユーザーに質問すること。

## その他

* アプリケーション起動時にバックグラウンドで `opencode acp` を実行すること。これがないとOpenCodeとチャットできない。
* 動作確認として[この本](./モダンHTML.pdf) を使っても良い。

